function goToFaction(faction) {
    alert("This will later link to the " + faction + " wiki page.");
}

function goToCustomWiki() {
    // later this will check login status
    window.location.href = "php/login.php";
}
